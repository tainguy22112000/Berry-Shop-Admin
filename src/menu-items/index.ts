import application from './application';
import dashboard from './dashboard';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [
    dashboard,
    application,
    // , utilities, other
  ],
};

export default menuItems;
