const BASE_ROUTER = '/';
const USERS_PAGE_ROUTER = '/users';
const ORDERS_PAGE_ROUTER = '/orders';
const LOGIN = '/login';
const PRODUCTS_PAGE_ROUTER = '/products';

const USERS_PATH = {
  Profile: 'profile',
  Account: 'account',
};

const ODERS_PATH = {
  OrderList: 'list',
  OrderDetails: 'details',
  DiscountCoupon: 'discount',
};

const PRODUCTS_PATH = {
  ProductDetails: 'details',
  ProductList: 'lists',
};

const DASHBOARD_PATH = {
  Dashboard: 'dashboard',
};

export {
  BASE_ROUTER,
  DASHBOARD_PATH,
  LOGIN,
  ODERS_PATH,
  ORDERS_PAGE_ROUTER,
  PRODUCTS_PAGE_ROUTER,
  PRODUCTS_PATH,
  USERS_PAGE_ROUTER,
  USERS_PATH,
};
